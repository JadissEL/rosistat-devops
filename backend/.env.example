# Backend environment variables example
# Copy to .env and set actual values for production
PORT=8080
NODE_ENV=production
# Path to sqlite file inside container or host
DB_FILE=/var/lib/rosistat/rosistat.db
# Optional values
# LOG_LEVEL=info
# TRUST_PROXY=true
